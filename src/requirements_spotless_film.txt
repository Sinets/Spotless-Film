# Spotless Film App Requirements
# Comprehensive dependencies for the Spotless Film application

# Core GUI and Windowing
customtkinter>=5.2.0
tkinterdnd2>=0.4.3

# Image Processing
Pillow>=9.5.0
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.11.0

# Deep Learning — U-Net + LaMa inpainting (IOPaint；PyPI 包名 IOPaint，import 名 iopaint；与 Pillow 9.5.x 由 pip 一并解析)
torch>=2.0.0
torchvision>=0.15.0
IOPaint>=1.5.0,<1.7

# Progress and UI
tqdm>=4.65.0

# File and Path Handling
pathlib2>=2.3.7; python_version < '3.4'

# Optional: Additional image formats
# Pillow-SIMD>=10.0.0  # SIMD-optimized version of Pillow

# Development and Testing (optional)
# pytest>=7.4.0
# black>=23.0.0
# flake8>=6.0.0

# Platform-specific optimizations
# For macOS (Metal Performance Shaders)
# No additional requirements - torch includes MPS support

# For CUDA support (NVIDIA GPUs)
# torch>=2.0.0+cu118
# torchvision>=0.15.0+cu118

# Note: Install instructions:
# 1. Basic: pip install -r requirements_spotless_film.txt
# 2. PyInstaller 离线 LaMa: 在构建前于 src/ 下运行 python scripts/fetch_lama_checkpoint.py，将 big-lama.pt 打入安装包
# 3. CUDA: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
